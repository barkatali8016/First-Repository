//get input from user

let arr=[];//declare empty array
let size=prompt("Enter the size of array=");//to store the size of array
for(let i=0;i<size;i++)
{
    arr[i]=prompt("Enter elements= ");//get the element from user
}
for(let i=0;i<size;i++)
{
    console.log( arr[i]);//displaying elememts
}
